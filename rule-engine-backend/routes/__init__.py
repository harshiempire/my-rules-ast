from .rule_routes import rule_bp
# The rule_routes blueprint contains functions for managing rules, such as creating, retrieving, updating, and deleting rules.
from .evaluation_routes import evaluation_bp
# The evaluation_routes blueprint contains functions for evaluating rules against input data, such as evaluating a single rule, combining multiple rules, or validating a rule string.
